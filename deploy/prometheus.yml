global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'beloved-controlplane'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['beloved-controlplane:3000', 'host.docker.internal:3000']
